# Open Decisions / TODO

Things intentionally left vague or unaddressed in current site copy, to revisit once decided.

- [ ] **Post-handoff support terms.** About page currently says only "if something delivered doesn't hold up the way it should, that's on Keystone Systems to fix, not on you to discover the hard way" — deliberately no specific window length or scope. Once decided (e.g. a 30-day bug-fix window for build/harden/automate/modernize engagements, and separate framing for Due Diligence as a point-in-time assessment rather than an open-ended guarantee), update:
  - `apps/web/app/about/page.tsx` (Approach section) with concrete terms
  - `docs/company-context.md` under "Bench depth and post-handoff support"
- [ ] **Bench depth specificity.** About page says only "a small network of equally experienced independent engineers" — no numbers, no names. Decide whether to ever be more specific in public copy (currently intentionally generic).
- [ ] **No social proof anywhere on the site.** No testimonials, case studies, or before/after examples. Add once real client examples exist and are cleared to share.
- [ ] **No stated recourse for a due-diligence report that misses something material.** Depends on the post-handoff support terms decision above — Due Diligence likely needs its own disclaimer language ("assessment based on information available during the engagement window") rather than a fix-window promise, since there's no code to fix.
